global:
  Brand: numenor
  BrandDomain: numenor.arda
  BrandRegion: arda
  Bucket:
    AppRepository: '${AWS::Region}-arda-numenor-app-repository'
    AppData: '${AWS::Region}-arda-numenor-app-data'
    Logs: '${AWS::Region}-arda-numenor-${EnvShort}-logs'
  HostedZoneNameEnv: '${EnvShort}.numenor.arda'
  HostedZoneNamePrivate: 'internal.numenor.arda'
  HostedZoneNameRegionEnv: '${AWS::Region}.${EnvShort}.numenor.arda'
